<% provide(:button_text, 'Sign Up') %>
<% provide(:img_src, "signup-image.jpg")%>
<div class="section">
    <div class="row">
        <div class="col-md-6">
            <h1 class="title align-items-center">Sign Up</h1>
            <%= render 'form', user: @user %>
        </div>

        <div class="col-md-6 signup-div">
            <%= image_tag(yield(:img_src), class: "signup")%>
        </div>
    </div> 
</div>
<br>
<br>
<!--<button class="btn new"><%= link_to 'Back', users_path %></button>-->
 <hr class="separate">